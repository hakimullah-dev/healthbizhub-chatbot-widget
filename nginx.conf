server {
    listen 80;
    server_name 82.25.104.70;

    # Serve the chatbot widget
    location /chat/ {
        alias /var/www/healthbizhub-chat/;
        index index.html;
        try_files $uri $uri/ /chat/index.html;
    }
}
